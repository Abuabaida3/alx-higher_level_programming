#!/usr/bin/bash
str = python is interpreted interactive, object-oriented programing\
        language that combine remarkable power with very clear syntax"
        str = str[39:67] + str[107 + 112] + str[:6]
        print(str)
